import React from 'react';

const CreateUser = ({onCreate}) => {
  return (
    <div>
      <input type="text" />
      <input type="text" />
      <button>등록</button>
    </div>
  );
};

export default CreateUser;